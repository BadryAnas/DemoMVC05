@model IEnumerable<ApplicationUser>

<div class="row justify-content-center align-items-center mt-5">
    <form class="col-md-8" asp-action="Index">
        <div class="form-row">
            <div class="col-12 col-md-8 mb-2">
                <input type="text" class="form-control" name="searchInp" placeholder="Search By UserName" />
            </div>
            <div class="col-12 col-md-4 mb-2">
                <input type="submit" value="Search" class="btn btn-success btn-block" />
            </div>
        </div>
    </form>
</div>


@*  <div class="alert alert-info text-center">
        <h4>@ViewBag.Message</h4>
    </div>

    <div class="alert alert-info text-center">
        <h4>@ViewData["ViewDataMessage"]</h4>
    </div>

    <div class="alert alert-info text-center">
        <h4>@TempData["TempDataMessage"]</h4>
    </div>
 *@
@if (Model.Count() > 0)
{
    <table class="table table-striped table-bordered table-hover">
        <thead class="thead-dark">
            <tr>
                <th>@Html.DisplayNameFor(e => e.Email)</th>
                <th>@Html.DisplayNameFor(e => e.UserName)</th>
                <th>@Html.DisplayNameFor(e => e.Id)</th>
                
                <th class="text-center">Details</th>
                <th class="text-center">Update</th>
                <th class="text-center">Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var role in Model)
            {
                <tr>

                    <td>@role.Email</td>
                    <td>@role.UserName</td>
                    <td>@role.Id</td>
                    <partial name="_ActionButtonPartialView" model="@role.Id.ToString()" />

                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-info text-center">
        <h4>No Users Available</h4>
    </div>
}
</div>

<style>
    .container {
        max-width: 1200px;
        margin: auto;
        padding: 20px;
    }

    h1 {
        color: #343a40;
    }

    .table {
        margin-top: 20px;
    }

    .btn {
        padding: 10px 15px;
        margin: 0 5px;
    }
</style>